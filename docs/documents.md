# Documents

Everything EMCargo produces is a **PDF**. Some documents are the genuine official
forms, filled in field by field. The rest are generated by EMCargo in a clean layout
with the correct fixed legal wording.

- [Which documents per transport mode](#which-documents-per-transport-mode)
- [Official forms, filled in](#official-forms-filled-in)
- [Generated documents](#generated-documents)
- [What is never filled in for you](#what-is-never-filled-in-for-you)
- [Signatures](#signatures)
- [The waybill doubles as the ADR transport document](#the-waybill-doubles-as-the-adr-transport-document)

## Which documents per transport mode

| Mode | Documents offered |
|---|---|
| **Road** | CMR, AVC waybill, packing list, delivery note — and for dangerous goods: placarding and marking sheet (ADR 5.3), package label and marking sheet (5.2), container/vehicle packing certificate (5.4.2), on-board documents list (ADR 8.1.2), equipment sheet (ADR 8.1.4/8.1.5) |
| **Rail** | CIM, packing list, delivery note — and for dangerous goods: placarding and marking sheet (RID 5.3), package label and marking sheet (5.2) |
| **Sea** | B/L or Sea Waybill shipping instructions, VGM declaration, IMO Multimodal DG Form, packing list, delivery note — and for dangerous goods: placarding and marking sheet (IMDG 5.3), package label and marking sheet (5.2) |
| **Inland waterway** | ADN transport document, VGM declaration, stowage plan (ADN 7.1.4.11.1), placarding and marking sheet (ADN 5.3), package label and marking sheet (5.2), on-board documents list (ADN 8.1.2), packing list, delivery note |
| **Air** | AWB shipping instructions, IATA Shipper's Declaration, packing list, delivery note |
| **Multimodal** | All of the above |

Documents that exist for dangerous goods only — the declarations and the ADR/RID/ADN
sheets and lists above — appear once a package is marked as dangerous goods. Road, rail,
sea and inland waterway are the released modes today; air and multimodal stay locked
until their checks are complete (see the [roadmap](../ROADMAP.md)).

## Official forms, filled in

These four are the real documents. EMCargo does not redraw them — it opens the
original file and writes your values into it.

| Document | Template | Source |
|---|---|---|
| **CMR consignment note** | `templates/forms/cmr.pdf` | IRU model 2007, four copies |
| **CIM consignment note** | `templates/forms/cim.pdf` | CIT CIM/CUV, 2019 edition |
| **IATA Shipper's Declaration** | `templates/forms/iata_dgd.pdf` | IATA open format |
| **AVC waybill** | `templates/forms/avc.pdf` | sVa / Stichting Vervoeradres |

The CMR, CIM and IATA forms have proper PDF form fields (AcroForm), so the values are
written straight into them.

The AVC form is a flat PDF with no fields at all. For that one EMCargo places the
values as a text layer on top of the template, at positions derived from the form's own
ruling and field labels. The grid is documented in
`backend/app/services/documents/avc_form.py`, and the tests check the actual coordinates
of the text — so if the overlay ever drifts, a test fails rather than a crooked form
being printed.

> If you fork this repository publicly, check the redistribution terms of each official
> form before you include the template files.

## Generated documents

Produced with reportlab in EMCargo's own layout, including the fixed legal wording
each document requires:

| Document | Notes |
|---|---|
| **IMO Multimodal Dangerous Goods Form** | Includes the shipper's declaration text |
| **VGM declaration** | SOLAS method 1 or 2, with a cross-check on the total |
| **AWB shipping instructions** | Handling information, Cargo Aircraft Only flag |
| **B/L or Sea Waybill shipping instructions** | |
| **ADN transport document** | Inland waterway — there is no waybill for this mode in the app |
| **Packing list** | |
| **Delivery note** | Receipt block left blank for signing on delivery |
| **[Structured shipment export (JSON)](shipment-export.md)** | Not paper but data: the whole shipment, including what EMCargo derived and against which editions. Offered on every transport mode. EMCargo's own format with its own version number — not an eFTI message and not an eCMR |
| **Package label and marking sheet (5.2)** | Which labels and marks each package carries, per regime, and the class labels themselves at full size — 100 mm on each side, one per A4 page. The artwork is the official one, cut from the ADR; nothing is redrawn. The battery mark and the orientation arrows are printed too: the arrows are the framed figure of 5.2.1.10.1.2, cut whole, and the battery mark is the edition's own symbol inside the frame 5.2.1.9.2 states in words — 100 mm square, 5 mm of red hatching — carrying the UN number where the figure prints an asterisk. A sea consignment also gets what column 6 of the Dangerous Goods List says about its labels: the effect each cited special provision would have, and whether it was applied or is left to be judged. Every page carrying a figure names BS 5609, the standard a material meets to survive the IMDG Code's three months' immersion — the working page carries the full note, and the pages that get cut up carry it in one line, because the working page is the one a packer throws away. Air is absent: the IATA marking rules have not been read |

## What is never filled in for you

EMCargo distinguishes four kinds of field, and only fills the first:

| Field type | Filled? |
|---|---|
| **Your input** | Yes — from what you entered |
| **Carrier data** | No — the carrier supplies it (vehicle registration, AWB number, booking) |
| **Operational** | No — filled at handover (arrival times, condition on receipt) |
| **Signature** | Only your own, and only if you draw or upload one |

A document that still needs carrier data shows as **Waiting for carrier data** on the
export screen. It can still be exported — it is a draft, after all.

## Signatures

Draw your signature with a mouse, finger or stylus, or upload an image (PNG, JPEG or
WebP — a white background is made transparent and the image is trimmed automatically).
You can also skip it entirely and sign on paper.

Draw it once under **Settings** and it is ready on every shipment from then on; you can
still replace or remove it per shipment. It is stored on your own server and nowhere
else — see [Privacy](privacy.md#what-is-stored).

Your signature appears in the sender's box: CMR box 22 on all four copies, the IATA
signature field, the AVC sender panel, and a signature section on generated PDFs.

Carrier and consignee signatures — CMR boxes 23 and 24, CIM box 61, the delivery note
receipt — are **always left blank**.

## The waybill doubles as the ADR transport document

ADR 5.4.1 does not prescribe a form for the transport document. A normal waybill is
enough, as long as it carries the right information in the right order.

So EMCargo puts it there. When a package contains dangerous goods, the CMR and the
AVC waybill carry the official description from ADR 5.4.1.1.1 in the goods column:

```
UN 1203, BENZINE OF MOTORBRANDSTOF (GASOLINE), 3, II, (D/E), 10 jerrycan, 200 L
```

and the total per transport category (5.4.1.1.1.1) required when you rely on the
1,000-point exemption. Packages without dangerous goods keep their normal description,
and the mass is not counted twice.

That is why there is no separate ADR road document: it would duplicate a waybill you
already have. Inland waterway still has its own ADN document, because there is no
waybill for that mode in the app.
